has changed status for "#{activity.parameters[:user_title]} (#{activity.parameters[:user_email]})" from "#{activity.parameters[:old_status]}" - group (#{activity.parameters[:old_group]}) to "#{activity.parameters[:new_status]}" - group (#{activity.parameters[:new_group]})